export enum Roles {
  SYSTEM_ADMIN = "System Admin",
  AIRWAY_REPRESENTATIVE = "Airway Representative",
  CUSTOMER = "Customer",
}

export enum Genders {
  MALE = "Male",
  FEMALE = "Female",
}

export enum Titles {
  MR = "Mr",
  MRS = "Mrs",
  MISS = "Miss",
}